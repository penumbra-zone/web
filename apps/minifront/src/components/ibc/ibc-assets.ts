/*
import { assets } from 'chain-registry';
import { AssetList as CosmosAssetList } from '@chain-registry/types';

import { testnetIbcChains } from '@penumbra-zone/constants/src/chains';
const supportedChainNames = testnetIbcChains.map(({ chainName }) => chainName);

export const cosmosTestnetAssets: CosmosAssetList[] = assets.filter(
  ({ chain_name }) => chain_name in supportedChainNames,
);
*/
