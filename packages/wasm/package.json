{
  "name": "wasm",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "compile": "wasm-pack build ./crate --release --target bundler --out-name index",
    "build": "tsc",
    "lint": "eslint \"**/*.ts*\"",
    "test": "vitest run"
  },
  "dependencies": {
    "bech32": "^2.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1",
    "fake-indexeddb": "^5.0.2",
    "tsx": "^4.7.0",
    "vitest": "^1.2.2"
  }
}
